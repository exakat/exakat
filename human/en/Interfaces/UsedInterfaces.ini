name = "Used Interfaces";
description = "Interfaces used in the code. 

<?php

interface used {}

// Used by implementation
class c implements used {}

// Used by extension
interface j implements used {}

$x = new c;

// Used in a instanceof
var_dump($x instanceof used); 

// Used in a typehint
function foo(Used $x) {}

?>

";
clearphp = "";
severity = "";
timetofix = "";
exakatSince = "0.8.4";
