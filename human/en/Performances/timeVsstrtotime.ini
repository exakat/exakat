name = "Performances/timeVsstrtotime";
description = "time() is actually faster than strtotime('now').

<?php

// Faster version
$a = time();

// Slower version
$b = strtotime('now');

?>

This is a micro-optimisation. Gain is real, but small unless the function is used many times.

";
clearphp = "";
