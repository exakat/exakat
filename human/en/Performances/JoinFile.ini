name = "Join file()";
description = "Applying join (or implode) to the result of file will provide the same results than file_get_contents(), but at a higher cost of memory and processing.

Always use file_get_contents() to get the content of a file as a string. ";
clearphp = "";
