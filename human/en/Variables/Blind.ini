name = "Blind variables";
description = "Variables that are used in foreach or for structure, for their managing the loop itself. In foreach($array as $key => $value) {}, $key and $value are blind variables, that may be used within the loop. ";
clearphp = "";
