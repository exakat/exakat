name = "Unescaped Variables In Templates";
description = "Whenever variables are emitted, they are reported as long as they are not escaped. 

While this is quite a strict rule, it is good to know when variables are not protected at echo time. 

<?php
    echo $unescapedVariable;
    
    echo esc_html($escapedVariable);

?>

";
clearphp = "";
severity = "S_MAJOR";
timetofix = "T_SLOW";
exakatSince = "0.8.4";
