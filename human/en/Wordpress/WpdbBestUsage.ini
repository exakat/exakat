name = "Wpdb Best Usage";
description = "Wordpress database API (\$wpdb) offers several eponymous methods to safely handle insert, delete, replace and update. 

It is recommended to use them, instead of writing queries with concatenations.";
clearphp = "";
