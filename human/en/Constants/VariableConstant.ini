name = "Variable Constants";
description = "Variable constants are actually constants whose value is accessed via the function constant(). Otherwise, there is no way to dynamically access a constant (aka, when the developper has the name of the constant as a incoming parameter, and it requires the value of it). ";
