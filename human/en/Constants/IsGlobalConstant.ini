name = "Is Global Constant";
description = "Mark a constant that may fallback to a global const definition, even though it is in an array. 

This will skip PHP and ext's functions, namespaced constants. ";
clearphp = "";
