name = "Could Be CIT";
description = "Mark arguments and return types that can be set to a class, interface definition.

<?php

// Accept an object as input 
function foo($b) {
    // Returns new object
    return new ($b->classname);
}

?>
";
clearphp = "";
phpversion = "";
severity = "S_MAJOR";
timetofix = "T_QUICK";
exakatSince = "2.1.2";
precision = "P_HIGH"
modifications[] = "Add the class or interface typehint to the code."
