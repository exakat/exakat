name = "Could Be Callable";
description = "Mark arguments and return types that can be set to ``callable``.

<?php

// Accept a callable as input 
function foo($b) {
    // Returns value as return
    return $b();
}

?>
";
clearphp = "";
phpversion = "";
severity = "S_MAJOR";
timetofix = "T_QUICK";
exakatSince = "2.1.2";
precision = "P_HIGH"
modifications[] = "Add `callable` typehint to the code."
