name = "Could Be String";
description = "Mark arguments and return types that can be set to string.

<?php

// Accept a string as input 
function foo($a) {
    // Returns a string
    return $a . 'string';
}

?>
";
clearphp = "";
phpversion = "";
severity = "S_MAJOR";
timetofix = "T_QUICK";
exakatSince = "2.1.2";
precision = "P_HIGH"
modifications[] = "Choose the string typehint, and add it to the code."