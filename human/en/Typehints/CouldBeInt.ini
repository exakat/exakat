name = "Could Be Integer";
description = "Mark arguments, properties and return types that can be set to ``int``.

<?php

// Accept an int as input 
function foo($b) {
    // Returns an int
    return $b + 8;
}

?>
";
clearphp = "";
phpversion = "";
severity = "S_MAJOR";
timetofix = "T_QUICK";
exakatSince = "2.1.4";
precision = "P_HIGH"
modifications[] = "Add `int` typehint to the code."
