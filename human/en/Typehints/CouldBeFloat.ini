name = "Could Be Float";
description = "Mark arguments, properties and return types that can be set to ``float``.

<?php

// Accept an int as input 
function foo($b) {
    // Returns a float (cubic root of $b);
    return pow($b, 1 / 3);
}

?>
";
clearphp = "";
phpversion = "";
severity = "S_MAJOR";
timetofix = "T_QUICK";
exakatSince = "2.1.4";
precision = "P_HIGH"
modifications[] = "Add `float` typehint to the code."
