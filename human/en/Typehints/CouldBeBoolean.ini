name = "Could Be Boolean";
description = "Mark arguments and return types that can be set to boolean.

<?php

// Accept a boolean as input 
function foo($b) {
    // Returns a boolean
    return $b === true;
}

?>
";
clearphp = "";
phpversion = "";
severity = "S_MAJOR";
timetofix = "T_QUICK";
exakatSince = "2.1.2";
precision = "P_HIGH"
modifications[] = "Add `bool` typehint to the code."
