name = "Concat Integers";
description = "PHP will fail if one try to concatenate an integer with a string (it seems it will try to understand this as decimal number).

Place the integer between apostrophes or in a variable. 
";
