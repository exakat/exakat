name = "Foreach which source is not a variable";
description = "Foreach are faster when applied to a variable, rather than the result of a function. Thus, $x = f(); foreach ($x as $y) {} is faster than foreach(f() as $y) {}.";
clearphp = "";
