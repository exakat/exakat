name = "Simple Regex";
description = "PRCE regex are a powerful way to search inside strings, but they also come at the price of performance. When the query is simple enough, try using strpos() or strposi() instead.";
clearphp = "";
