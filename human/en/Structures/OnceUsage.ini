name = "include_once() usage";
description = "All the _once inclusion functions should be avoided for performances reasons.

Try using auto_load() for loading classes, or using include() and make it possible to include several times the same file without errors.
";
clearphp = "";
