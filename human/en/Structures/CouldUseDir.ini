name = "Could Use __DIR__";
description = "Use __DIR__ function to access the current file's parent directory. 

<?php

assert(dirname(__FILE__) == __DIR__);

?>

__DIR__ has been introduced in PHP 5.3.0.

";
clearphp = "";
