name = "var_dump() or print_r() Usage";
description = "var_dump() and print_r() are debugging functions, that should not be left in any production code. 

They are OK to use during development time, but must be removed so as not to have any chance to be run in production.
";
