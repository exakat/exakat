name = "Eval Without Try";
description = "Eval() emits a ParseError Exception with PHP 7 and later. Catching this exception is the recommended way to handle errors while using the eval function.";
clearphp = "";
