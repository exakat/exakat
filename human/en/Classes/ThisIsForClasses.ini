name = "$this belongs to classes";
description = "$this variable represents an object (the current object) and it should be used within class\'s methods (except for static) and not outside.";
clearphp = "";
