name = "Static Names";
description = "List of properties and methods defined as static.

<?php

class X {
    // This is a static property
    static $x = 1;
    
    // This is a static method
    static function foo() {
        // This is not a static variable
        static $variable;
    
        // This is a not a static property
        if ($a instanceof static) {
            return;
        }
    }
}
?>
";
clearphp = "";
exakatSince = "0.8.4";
