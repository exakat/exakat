name = "Has Magic Property";
description = "This class has defined one of the magic methods : __call(), __callStatic(), __get(), __set(), __isset(), __unset(), __sleep(), __wakeup(), __toString(), __invoke(), __set_state(), __clone() and __debugInfo().
__construct() and __destruct() are not counted here. 

<?php

class WithMagic {
    // some more methods, const or properties
    
    public function __get() {
        // doSomething();
    }
}

?>

";
clearphp = "";
exakatSince = "0.8.4";
