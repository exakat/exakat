name = "Can't Disable Function";
description = "This is the list of potentially dangerous PHP functions being used in the code, such as exec() or eval(). 
This is the base for suggesting disable_functions directive.";
clearphp = "";
