name = "Multiple returns";
description = "In terms of maintenability, it is recommended to have only one return statement by function or method. 

With multiples returns, some of them are probably shortcuts and will require more work when the code needs to be refactored.";
