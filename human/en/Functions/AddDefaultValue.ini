name = "Add Default Value";
description = "Parameter in methods definition may receive a default value. This allows the called method to set a value when the parameter is omited. 

<?php

function foo($i) {
    if (!is_integer($i)) {
        $i = 0;
    }
}

?>

";
clearphp = "";
exakatSince = "1.4.5";