name = "Spip Include";
description = "With spip, inclusion are made using the function 'include_spip', which works just like PHP inclusion. The only exception is for including the file 'inc_version.php', which define the include_spip() function.";
