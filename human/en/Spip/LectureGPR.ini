name = "GET POST REQUEST Reading";
description = "$_GET $_POST and $_REQUEST must be accessed using the function _request() and the name of the incoming variable as argument. 

$_GET $_POST and $_REQUEST must not be used directly, outside the _request() function.
";
