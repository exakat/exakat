name = "Unused *_Dist";
description = "Defined *_dist functions, that are not called in charger_fonction().

Note that dynamic call (like charger_fonction($fonction, $folder)) are not taken into account here. ";
