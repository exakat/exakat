name = "Use Zend Session";
description = "Zend sessions are being used.

<?php

    //Getting the session manager from the application
   $session = $e->getApplication()
                ->getServiceManager()
                ->get('Zend\Session\SessionManager');

    //a service manager is already available
    $serviceManager->get('Zend\Session\SessionManager');

?>

";
clearphp = "";
severity = "";
timetofix = "";
exakatSince = "0.10.4";