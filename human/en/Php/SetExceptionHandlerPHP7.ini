name = "set_exception_handler() Warning";
description = "set_exception_handler() callable function has to be adapted to PHP 7 : Exception is not the right typehint, it is now Throwable. 

When in doubt about backward compatibility, just drop the Typehint. Otherwise, use Throwable.";
clearphp = "";
