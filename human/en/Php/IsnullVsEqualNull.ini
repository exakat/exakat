name = "Use === null";
description = "It is faster to use === null instead of is_null().

<?php

// Operator === is fast
if ($a === null) {

}

// Function call is slow 
if (is_null($a)) {

}


?>

";
clearphp = "avoid-those-slow-functions";
severity = "S_MINOR";
timetofix = "T_QUICK";
exakatSince = "0.8.4";

modifications[] = "Use === comparison"