<?php

	if ( isset($a['B']) && isset($b['C']) && isset($c['D']) ) :

		if ( $d->E == $e && $f->F == $g && G( $h->H, array( 'I', 'J' ) ) && $i->K( 'L', 'M' ) ) :

						$j->N( O('P', 'Q' ) );

						$k->R( S( 'T', 'U' ) );

			V( 'W', $l->X );

		elseif ( $m->Y != 'Z' ) :

			$n->AA( AB( 'AC', 'AD' ) );

		else :

			$o->AE( AF( 'AG', 'AH' ) );

		endif;

		AI( AJ( AK( 'AL' ) ) );
		exit;

	endif;    
?>