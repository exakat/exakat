<?php

switch ($a['B']) {
	case 'C' :
			$b = D::E;
			$c = $d;
			if (isset($e->F)) {
				$f = $g->G;
				$h = $i->H($j['I']);
				$k = $l['J'];
			}
			break;
	case 'K' :
			$m = L::M;
			$n = ($o == 'N') ? O : P;
			break;
	case 'Q' :
			$p = R::S;
			$q = (float) $r['T'];
			if (U($s) == $t) {
				$u = (integer) $v;
			}
			$w = V::W;
			break;
	case 'X' :
			$x = Y::Z;
			$y = (float) $z['AA'];
			if (AB($aa) == $ab) {
				$ac = (integer) $ad;
			}
			$ae = AC::AD;
			break;
	case 'AE' :
			$af = AF::AG;
			$ag = (float) $ah['AH'];
			if (AI($ai) == $aj) {
				if ($ak = (integer) $al)
					$am = (integer) $an;
				else
					$ao = (float) $ap;
			}
			break;
	case 'AJ' :
			$aq = AK::AL;
		    $ar = new AM($as['AN'], $at);
			$au->AO($av);
			list($aw,$ax,$ay,$az,$ba,$bb) = AP('AQ',$bc->AR('AT'));
			$bd = AU::AV($be,$bf,$bg,$bh,$bi,$bj);
			if ($bk != AW($bl)) {
				$bm = AX::AY.'AZ'.BA::BB;
			} else {
				$bn = BC::BD;
			}
			break;
	case 'BE' :
			$bo = BF::BG;
			$bp = BH::BI(BJ('BK'.BL('BM',BN($bq['BO'],'BP'))));
			$br = BQ::BR;
			break;
}
