<?php

if ( 0 ) :
	if ( !empty( $a['B'] ) && 'C' === $a['B'] ) :
		E( 'F' );
		E( 'H', 'I' );

		elseif ( !empty( $a['B'] ) && 'K' === $a['B'] ) :
		M( 'N' );
		M( 'H', 'Q' );

		else :
		R( 'S' );
		T( 'H', 'V' );

	endif;

else:
	W( 'X' );
	W( 'Z', 'AA' );
endif;
?>
