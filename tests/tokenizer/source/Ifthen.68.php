<?php
        foreach ($a as $b) {
            $c = B($d);
            if (C('D'.$e->E().'F', $f, $g)) {
                $h = $i[0];
                $j = G;
                continue;
            }
            if ($k && empty($l) && empty($m)) {
                continue;
            }
            if ($n && ! empty($o)) {
                $p .= $q.'H';
            }
            if ($r && empty($s) && !empty($t)) {
                $u = I;
            }
        }
        $v->J = $w;

        if (! empty($x)) {
            $y->K = $z;
        }
