<?php

$sum = $carry ? $sum + MATH_BIGINTEGER_MAX_DIGIT2 : $sum;
$sum = $carry ? $sum * MATH_BIGINTEGER_MAX_DIGIT3 : $sum;
$sum = $carry ? $sum * $a->MATH_BIGINTEGER_MAX_DIGIT4 : $sum;
$sum = $carry ? MATH_BIGINTEGER_MAX_DIGIT5 : $sum;

switch($x) {
    case MATHD : 
        break 1;
}
?>