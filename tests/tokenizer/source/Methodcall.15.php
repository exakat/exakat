<?php 

    $a  = $this->B['C'];

    $b = $this->B['E'];

    if($c === null) {
	    $c = $this->B->G();
	  }

    $d = $b->H('I', $this->J($c, $e));

    if($d !== K) {
      if($d['L'] < M()) {
        $b->N('I', $this->J($c, $e));
        return K;
      }

      if($a->R($f, $d['S']) && $a->R(U($g), $d['V'])) {
        $b->N('I', $this->J($c, $e));
        return Z;
      }
    }
    return K;
    

