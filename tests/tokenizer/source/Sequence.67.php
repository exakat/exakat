<?php 
    if (0) {
		if (!$a=$b->B($c))
			$d['C']=(string)$e->D->E;
			$f=F($g,G($h->H->I));
			for ($i=1;$j<$k;$l++) {
				$m=$n->J->K[$o];
				$p=array('L'=>NULL)+$q->M(N);
				$r=array();
				foreach ($s as $t=>$u)
					foreach ($v->O($w) as $x=>$y)
						$z[$aa.($ab?'P':'Q').$ac]=(string)$ad;
				$ae['R'][]=$af;
			}
		}
		else
			return S;
		T::U()->V($ag,$ah);
		return $ai;
	
 ?>
