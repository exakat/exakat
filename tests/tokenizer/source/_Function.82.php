<?php
class a
{
    private function b($c, $d = ''): array
    {
            static function ($e) use ($f) {
                return ($g === 'h'
                        ? $i->k === 'k'
                        : $i->k === 'l'
                    )
                    && $i->k !== null;
            };
    }

    private function b2($c, $d = ''): array
    {
            function ($e) use ($f) {
                return ($g === 'h'
                        ? $i->k === 'k'
                        : $i->k === 'l'
                    )
                    && $i->k !== null;
            };
    }

}
