<?php
        try {
            $a = new \B(
                new \C($b, \D::E),
                \B::G
            );

            
            foreach ($a as $c) {
                if ($c->H() == 'I') {
                    continue;
                }

                $c->J() ? @K($c->L()) : @M($c->L());
            }
        } catch (\O $d) {
                                };

        if ($e) {
            @M($b);
        }
        
