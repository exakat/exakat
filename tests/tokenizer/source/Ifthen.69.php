<?php

		if ($a->B=='C') :

			if (D($b->E)) :
				if ( ! F( $c['G']['H'], $d->I) ) return J;
			endif;

		elseif ($e->K=='L') :

			if (M($f->N)) :
				if ( ! O( $g['P']['Q'], $h->R) ) return S;
			endif;

		else :

			if (T($i->U)) :
				if ( V( $j['W']['X'], $k->Y) ) return Z;
			endif;

		endif;

    
?>