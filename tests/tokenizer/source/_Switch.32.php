<?php

		switch ($a) {
		case "b":
			$c = $d->e($f);
			if ($g) return $h;

			list(, $i, $j) = $k;
			if ($l == "%") $m = "n";
			$o = isset($p->q[$r]) ?
				$s->t[$u] : array($v, 'w'.$x);

			if (is_callable($y)) { }
		}

?>