<?php
		return $this->parts[] = $part === 1 ? new self : $part;
		return $this->parts[] = $part === NULL ? new static : $part;
		return $this->parts[] = $part === NULL ? new parent : $part;
		return $this->parts[] = $part === NULL ? new X : $part;
		return $this->parts[] = $part === 1 ? new self() : $part;
		return $this->parts[] = $part === 1 ? new self(1) : $part;
		return $this->parts[] = $part === 1 ? new self(1, 2) : $part;

?>