<?php

$expected     = array('shell_exec($d)',
                      'shell_exec($d2)',
                      'shell_exec($d3)',
                      'shell_exec($d4)',
                     );

$expected_not = array('shell_exec($dd5)',
                      '',
                     );

?>