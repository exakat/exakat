<?php

foreach($a as $b) {
    preg_match($c, $b);
}

foreach($A as $B) {
    \preg_match($C, $B);
}




foreach($a1 as $b1) {
    preg_match($c1, 'a', $b1);
}

foreach($a2 as $b2) {
    preg_match_all($c2, $b2);
}

?>