<?php

for($i = 0; $i < 10; $i++) {
    $a = array_merge_recursive($a, $b[$i]);
}

foreach($c as $b) {
    $o->p = array_merge_recursive($o->p, $b);
}

while($dw > 0) {
    A::$p = array_merge_recursive($a, A::$p);
}

do {
    $a[1] = array_merge_recursive($a[1], $b);
} while($ddo > 0);

$a = array_merge_recursive($a, $b);

do {
    $a[1] = $b;
} while($xxx > 0);

?>