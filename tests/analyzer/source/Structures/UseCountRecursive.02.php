<?php
foreach ($switch as $key => $val) {
	$row[$key] = $switch[$key][($count + count($val) -1) % count($val)];
}

foreach ($switch as $key => $val2) {
	$a = count($d) + $val2;
}

foreach ($switch as $key => $val4) {
	$a = count($val4) + $d;
}

foreach ($switch as $key => $val3) {
	$val3 += count($d);
}

?>