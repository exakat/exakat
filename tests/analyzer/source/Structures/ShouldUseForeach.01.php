<?php

    for ($index = 0; $index < sizeof($lines); $index++)
    {
        $line = $lines[$index];
    }
    
    for ($index1 = 1; $index < sizeof($lines); $index++)
    {
        $line = $lines[$index1];
    }
    
    for ($index2 = 1, $index3 = 0; $index < sizeof($lines); $index++)
    {
        $line = $lines[$index3];
    }
    
    for ($index4 = 0; $index < sizeof($lines); $index++)
    {
        $line = $lines[$index4];
    }
    
    for ($index5 = 0; $index < sizeof($lines); $index5++)
    {
        $line = $lines[$index];
    }
    
    for ($index6 = 0; $index < sizeof($lines); ++$index6)
    {
        $line = $lines[$index6];
    }
?>
