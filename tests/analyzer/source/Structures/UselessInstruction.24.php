<?php
        for ($len; $len > 0; $len--) {
            $str .= '.0';
        }

        for ($len = 2; $len > 0; $len--) {
            $str .= '.0';
        }

        for ($len2, $array[2], $o->m, A::C; $len > 0; $len--) {
            $str .= '.0';
        }
