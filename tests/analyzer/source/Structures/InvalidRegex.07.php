<?php
preg_match($a->_regexDelimiter . '^' . $a->g[$d] . '$' . $a->f . 'iu', $d);
preg_match_all("/<($b)" . X . "(>(.*)<\/$b>|(\/)?>)/siU", $string, $matches, PREG_SET_ORDER | PREG_OFFSET_CAPTURE);
preg_match("{$a}", $b, $c);
preg_split($pattern . $modifiers, $subject, $limit, $flags);
preg_match($this->F( ) . 'D', $FF);
preg_replace_callback("/" . $regexp[C] . "/{$regexp[D]}", array($this, 'method'), $x);

preg_replace_callback("/" . $regexp[C] . "/Z", array($this, 'method'), $x);
?>