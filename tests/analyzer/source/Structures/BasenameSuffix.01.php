<?php

    iconv_substr(basename($path), 0, 1);
    substr(basename($path), 0, 1);
    mb_substr(basename($path), 0, 1);
    str_replace(a, b, basename($path));

    substr(1, basename($path), 1);
    substr(1, $basename);

    $z->mb_substr(basename($path), 0, 1);

?>