<?php

foreach($code as $k => &$v) {
    $v = strtolower($v);
}

foreach($code as $k => $v2) {
    $v2 = strtolower($v2);
}

foreach($code as $k => $v3) {
    $v = strtolower($v);
    $a->b['c']($d, false, $e - 1);
}

?>