<?php

function foo($x) {
        $for = new stdClass();
        $Empty[1] = preg_replace('/ASD/', '', $x[0]);
        if (empty($Empty[1])) {
            error('B', '');
        }
        
        $b = $Empty[1] + 1;
}

function foo2($x) {
        $for = new stdClass();
        $Empty2[3] = preg_replace('/ASD/', '', $x[0]);
        if (empty($Empty2[3])) {
            error('A', '');
        }
        
}

?>