<?php

function foo() {
        $a = $b ? yield 1 : yield 2;
        $a = $b ? $c[1] + 1 : $c[1] + 1;
}

        $a = $b ? function ($a) {} : function ($b) {};

        if ($b) {
            foreach($a as $b) {}
        } else {
            foreach($a as $b) {}
        }

?>