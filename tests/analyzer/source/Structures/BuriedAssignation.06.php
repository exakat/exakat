<?php

        foreach ($a as $b) {
            if (! is_dir($c .= "/{$d}")) {
                mkdir($e);
            }

            if ( is_link($c2 = "/{$d2}")) {
                mkdir($e);
            }
        }

?>