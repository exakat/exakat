<?php

// This is not a sequence inside the {}
$object->{$this->_leftindex} = $node->{$this->_rightindex} + 1;

//function x() {$this->_leftindex}
// beware of the NEW (not a function call, but a class craetion!)
				$o = new $ext(NULL, isset($this) ? $this : NULL);

// ???
$this->parent['id'];

$a1->parent['id'];
$$a->parent['id'];
${$a}->parent['id'];

$a1->m('id');
$$a->m('id');
${$a}->m('id');

