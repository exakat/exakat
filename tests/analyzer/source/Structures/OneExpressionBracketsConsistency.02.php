<?php

foreach($a2 as $b)  $c++; 
foreach($a1 as $b) { $c++; }
foreach($a1 as $b) { $c++; }
foreach($a1 as $b) { $c++; }
foreach($a1 as $b) { $c++; }
foreach($a1 as $b) { $c++; }
foreach($a1 as $b) { $c++; }
foreach($a1 as $b) { $c++; }
foreach($a1 as $b) { $c++; }
foreach($a1 as $b) { $c++; }
foreach($a1 as $b) { $c++; }

?>