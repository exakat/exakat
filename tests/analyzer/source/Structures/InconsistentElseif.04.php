<?php

if($a->status === self::UNAVAILABLE) { /**/ } 
elseif($a->status === self::UNCHECKED) { /**/ }

if($a->status === self::UNAVAILABLE) { /**/ } 
elseif($b->status === self::UNCHECKED) { /**/ }

if($a->status === self::UNAVAILABLE) { /**/ } 
elseif($or === self::UNCHECKED) { /**/ }
?>