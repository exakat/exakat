<?php

foreach($f->g as $h0) {
    $a->b['c'][] = $h0->e;
}

// Includes a if() test
$bColumn = array();
foreach($a as $k => $v) {
    switch($v['a']) {
        case 1: 
            $bColumn[] = $v['b'];
            break;
        default : 
            $bColumn[] = $v['b'];
    }
}


?>