<?php
    function a ( ) {

        $a = function ( $a, Array $b ) { return in_array($a, $b); };
        $b = function ( $a2, Array $b2 ) { return in_array($a, $b) || in_array($b, $a); return b; };

        return 1;
    }

    function ab ( ) {

        return in_array($a, $b);

        return 1;
    }
?>