<?php

class x {
    function cc($id, $servers) {
        $servers = array_udiff($servers, $a->b(), array($this, 'c'));
    }

    function None() {
        if (!$this->hasResource($id)) {
            return $this->setResource($id, array(
                'servers' => $servers
            ));
        }
    }

    function OneButNotArg() {
        return $setResource($id, array(
            'servers' => $servers
        ));
    }

    function OneAndOk($b) {
        return array_map($b, range(0, 10));
    }
}

?>