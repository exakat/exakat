<?php

class B {
    public static function I() {
        $a[] = self::$b($c['d']);
        $a[] = static::$be($ce['de']);

        $a[] = static::${bef};

        $a[] = self::B($c['d']);
        $a[] = static::BE($ce['de']);


        $a[] = self::$g;
        $a[] = self::$g1[2];
        $a[] = static::$h;
        $a[] = static::$h1[2];
    }
}